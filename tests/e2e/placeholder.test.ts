import { describe, it } from "vitest";

// Placeholder for future Playwright-based end-to-end coverage.
// Keeping this skipped ensures the test suite reminds contributors
// that an automated flow should open, upload fixtures, and validate UI states.

describe.skip("e2e: instagram-follower-insights flow", () => {
  it("should parse exports and surface metrics", () => {
    // To be implemented with Playwright.
  });
});
